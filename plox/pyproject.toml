[project]
name = "plox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "engson", email = "corastweb94@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "frozendict",
]

[project.scripts]
plox = "src:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = true

[tool.uv.build-backend]
module-name = "src"
module-root = ""
